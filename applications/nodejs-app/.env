# 数据库配置（使用Docker MySQL）
DB_HOST=mysql
DB_PORT=3306
DB_USER=testuser
DB_PASSWORD=test123
DB_NAME=web3

# 应用配置
PORT=3000
NODE_ENV=development

# JWT配置（如果你的应用使用JWT）
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=24h

# 其他配置
CORS_ORIGIN=*